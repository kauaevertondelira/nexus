services:
  mqtt:
    image: eclipse-mosquitto:2
    container_name: nexus-mqtt-local
    restart: unless-stopped
    ports:
      - "127.0.0.1:1883:1883"
    volumes:
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro

